export default class Session{
    constructor(id, idPsicologo, idTurno, idEmpleado){
        this.id = id,
        this.idPsicologo = idPsicologo,
        this.idTurno = idTurno
        this.idEmpleado = idEmpleado
    }
}